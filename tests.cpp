

#include "pch.h"

// Test cases written in Visual Studio
// Comment out test cases to run puzzlemain.cpp via algorithm
#define CATCH_CONFIG_MAIN  // This tells Catch to provide a main() - only do this in one cpp file
#include "catch.hpp"
#include "training_data.h"
#include "test_label_reader.h"
#include <string>

// Evaluates given example board
//TEST_CASE("Probability Assertion") {
	//Model train_data;
	//train_data.Train();
	//REQUIRE(train_data.ConfusionMatrix(), .767)
//}
// tests the test method returns the correct size when trian called
//TEST_CASE("TestingModelSize") {

	//Model test;
	//REQUIRE(test.train().size() == 5000);

//}